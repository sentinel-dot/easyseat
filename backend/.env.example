# Umgebung (development | production)
NODE_ENV=development

# Datenbank – Option A: Einzelne Variablen (lokal oder Railway Reference)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=easyseatdb

# Datenbank – Option B: URL (z. B. Railway MYSQL_URL / MYSQL_PRIVATE_URL)
# Wenn gesetzt, überschreibt DB_HOST/DB_USER/DB_PASSWORD/DB_NAME/DB_PORT
# MYSQL_PRIVATE_URL=mysql://user:password@host:3306/database
# MYSQL_URL=mysql://user:password@host:3306/database

# Server-Port (Railway setzt PORT automatisch)
PORT=5001

# Frontend-URL(s) für CORS. Eine URL oder mehrere komma-getrennt (Vercel + Previews).
FRONTEND_URL=http://localhost:3000
# FRONTEND_URLS=https://easyseat.vercel.app,https://easyseat-xxx.vercel.app

# JWT – In Production PFLICHT: starkes Geheimnis (z. B. openssl rand -base64 32)
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=24h
